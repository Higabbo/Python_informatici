# Step 1: Definire i voti
scores = [75, 90, 75, 80, 90, 80, 75, 100, 85]

# Step 2: Calcolare la somma totale dei voti
total_sum = sum(scores) # Somma: 75 + 90 + 75 + 80 + 90 + 80 + 75 + 100 + 85
# print("Somma totale dei voti:", total_sum)

# Step 3: Calcolare il numero di voti
num_scores = len(scores) # Numero di voti: 9
print("Numero totale di voti:", num_scores)

# Step 4: Calcolare la media
average_score = total_sum / num_scores # Media: 750 / 9
print("Media generale:", average_score)